#include "BlockScheduler.h"

#include "app/Transport.h"

namespace app {
using transport::TransportBlockInfo;

void runBlockScheduler(AppContext* app, const TransportBlockInfo& blockTime) {
  (void)app;
  (void)blockTime;
}

} // namespace app
